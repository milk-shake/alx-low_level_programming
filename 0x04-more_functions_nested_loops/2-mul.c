#include "main.h"
#include <stdio.h>
/**
 * mul - multiplies two numbers either float or integers
 * @a:  is the int that will use for the argument of the function
 * @b:  is the int that will use for the argument of the function
 *
 * Return: Always return 0.
 */
int mul(int a, int b)
{
	mul(a, b);
	putchar ('\n');
	return (0);
}
