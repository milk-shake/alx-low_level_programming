#include "main.h"
#include <stdio.h>
/**
 * _strlen -Write a function that returns the length of a string.
 *
 * @s: character pointer s for string
 * Return:0.
 */
int _strlen(char *s)
{
	 char str[20];
	 int i;

	 s = str;
	while (*s != '\0')
	{
		i++;
		s++;
	}
	return (0);
}
