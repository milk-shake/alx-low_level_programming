#include "main.h"
#include <stdio.h>
/**
 * _strlen -Write a function that returns the length of a string.
 *
 * @s: character pointer s for string
 * Return:0.
 */
int _strlen(char *s)
{
	int len;

	 s = "My first strlen!";
	 len = _strlen(s);
	 _putchar(len);
	return (0);
}
