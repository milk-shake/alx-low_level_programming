#include "main.h"
/**
 * is_palindrome - returns 1 if palindrome and 0 if not
 * @s: string to check
 * Return: 1 if palindrome and 0 if not
 */
int is_palindrome(char *s)
{
	int i;

	i = _strlen(s) - 1;
	return (is_palindrome(s), i);
}
