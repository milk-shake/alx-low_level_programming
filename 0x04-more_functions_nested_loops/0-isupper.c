#include "main.h"
#include <stdio.h>

/**
 * _isupper - function to checks for uppercase character
 * @c:is the character that will use for the aurgument of the
 * function
 * Return: 0
 */
int _isupper(int c)
{

	if (c == 'A')
	{
		return (1);
	}
	else
		return (0);
	putchar('\n');
}

